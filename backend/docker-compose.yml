services:
  backend:
    image: pro-tech-suite-backend:latest
    container_name: pro-tech-suite-backend
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "5000:5000"
