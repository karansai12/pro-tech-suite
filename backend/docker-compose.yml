services:
  backend:
    image: pro-tech-suit-backennd:latest
    container_name: pro-tech-suit-backennd
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "5000:5000"
